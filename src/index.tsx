import * as React from "react";
import * as ReactDOM from "react-dom";
import { App } from "./App";

ReactDOM.render(
  <App lang="TypeScript" userName="React user" />,
  document.getElementById("output")
);